from sqlalchemy import create_engine

engine = create_engine(
    "mssql+pymssql://sa:5tgb%TGB5tgb@sqlserver-demo:1433/kratos"
)

df.to_sql(
    "sample_table",
    engine,
    if_exists="replace",
    index=False
)
